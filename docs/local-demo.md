# Local Demo

Run SQLite with `sqlite3 catalyst_data.db < schema.sql` and `sqlite3 catalyst_data.db < queries.sql`. Run Python with `python python/generate_data_brief.py examples/sample_project.json outputs/generated_brief.md`.
